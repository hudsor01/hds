---
description: use this rule for project structure and eliminate duplication.
globs: *.ts, *.tsx, *.json, *.js, *.jsx, *.sql
---

# Your rule content

- You can @ files here
- You can use markdown but dont have to

when providing terminal cli prompts, provide the prompts using yarn as the package manager.

when creating files for utils, only place them in the ./utils/ directory
when creating files for hooks, only place them in the ./hooks/ directory
when creating files for lib, only place them in the ./lib/ directory
when creating files for components, only place them in the ./components/ directory
when creating files for types, only place them in the ./types/ directory
when creating files for auth, only place them in the ./auth/ directory
when creating files for middleware, only place them in the ./middleware/ directory
when creating files for prisma, only place them in the ./prisma/ directory
when creating files for supabase, only place them in the ./supabase/ directory
when creating files for emails, only place them in the ./emails/ directory

